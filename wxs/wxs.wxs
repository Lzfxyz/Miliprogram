function priceFormat(price,number){
    var price = (typeof(price) == number ? price:parseFloat(price))
    var number = number || 2
    return price.toFixed(number)
}

module.exports = {
    priceFormat:priceFormat
}